# Config

Central settings currently live in `core/config.py`.

This folder is reserved for editable environment files or user presets if you want to split runtime values out of code later.

Current local data files:

- `whatsapp_contacts.example.json` - sample WhatsApp contact book
- `whatsapp_contacts.json` - your local contact book, ignored by git
